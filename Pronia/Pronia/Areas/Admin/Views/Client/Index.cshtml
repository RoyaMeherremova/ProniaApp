@model List<Client>
@{
    ViewData["Title"] = "Client";
}

<div class="container my-5">
    <a class="btn btn-success text-white" asp-action="Create" style="float:right">Create</a>
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        Image
                    </th>
                    <th>
                        Name
                    </th>
                    <th>
                        Operations
                    </th>

                </tr>
            </thead>
            <tbody>
                @foreach (var client in Model)
                {
                    <tr>
                        <td class="py-1">
                            <img src="~/assets/images/website-images//@client.Image" style="width:70px;height:70px" alt="image" />
                        </td>
                        <td class="py-1">
                            @client.Name
                        </td>


                        <td>
                            <a class="btn btn-outline-info" asp-action="Detail" asp-route-id="@client.Id">Info</a>
                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@client.Id">Edit</a>
                            <form style="display:inline-block" asp-action="Delete" asp-route-id="@client.Id" method="post">
                                <button type="submit" class="btn btn-outline-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                }


            </tbody>
        </table>
    </div>
</div>